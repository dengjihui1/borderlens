# BorderLens working agreements

- 这是高风险、时效敏感的旅行证件工具；任何新增规则必须有当前官方一手来源。
- 不允许模型凭记忆生成实时签证或入境结论；未证明的情况必须输出 `review`。
- 规则应保持窄、可测试、可追溯，每条结论包含来源与核验日期。
- 不收集护照号码、证件图片、完整出生日期、住址或其他非必要个人数据。
- 不实现自动签证办理、付款、材料上传、发送或提交。
- 修改共享规则时更新测试和 `docs/RULES_AND_SOURCES.md`。
- 发布前运行 `npm test`、`npm run lint`、`npm run build`。
